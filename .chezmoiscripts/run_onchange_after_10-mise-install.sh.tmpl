#!/bin/bash
# config hash: {{ include "dot_config/mise/mise.toml" | sha256sum }}
# lock hash: {{ include "dot_config/mise/mise.lock" | sha256sum }}

set -euo pipefail

MISE="{{ if $m := lookPath "mise" }}{{ $m }}{{ else }}{{ .chezmoi.homeDir }}/.local/bin/mise{{ end }}"

$MISE trust {{ .chezmoi.homeDir }}/.config/mise
$MISE install --yes
